logging.level:
  org.springframework.cloud.gateway: TRACE
  org.springframework.security: TRACE
  org.springframework.web.reactive.function.client: TRACE

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://127.0.0.1:6543
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: "*"
            allowedHeaders: "*"
            allowedMethods:
              - GET
              - POST
      routes:
        - id: point
          uri: http://127.0.0.1:80
          predicates:
            - Path=/point/**
          filters:
            - RemoveRequestHeader=Cookie
        - id: oauth
          uri: http://127.0.0.1:6543
          predicates:
            - Path=/oauth2/**